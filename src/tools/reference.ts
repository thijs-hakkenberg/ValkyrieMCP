/** Search game content catalogs (monsters, tiles, items, etc.) */
export function searchGameContent(query: string, type?: string): any[] {
  // Catalogs not yet extracted - return empty for now
  return [];
}
